# Deploy to GitHub Pages

1. Create a new public repository, for example `1688-copilot-validation`.
2. Upload all files in this folder to the repository root.
3. Open repository **Settings → Pages**.
4. Under **Build and deployment**, choose **GitHub Actions**.
5. The included workflow publishes the site after the first push to `main`.

No paid service, API key, domain, or backend is required.
